def transp(supply, demand, cost):
    """
    Args:
        supply: list of integers, representing the amounts available at each origin
        demand: list of integers, representing the amounts required at each destination
        cost: 2D list of integers, representing the shipment costs per unit from each origin to each destination
        
    Returns:
        total_cost: an integer, representing the minimized total cost of transportation
    """
    # To be implemented
    total_cost = 0
    return total_cost