def uncapacitated_lot_sizing_backlogging(periods, demand, cost_fixed, cost_unit_order, cost_unit_hold, penalty):
    """
    Args:
        periods: a list of integers, indicating the number of periods
        demand: a list of integers, indicating the demand in each period
        cost_fixed: a list of integers, indicating the fixed ordering cost in each period
        cost_unit_order: a list of integers, indicating the unit ordering cost in each period
        cost_unit_hold: a list of integers, indicating the unit holding cost in each period
        penalty: a list of integers, indicating the unit backlogging penalty in each period

    Returns:
        total_cost: an integer, representing the minimized total cost
    """
    # To be implemented
    total_cost = 0
    return total_cost