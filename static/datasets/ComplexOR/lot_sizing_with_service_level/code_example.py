def lot_sizing_with_service_level(demand, cost_fixed, cost_unit_order, cost_unit_hold, penalty, service_level):
    """
    Args:
        demand: list of integers, representing the demand in each period
        cost_fixed: list of integers, representing the fixed ordering cost in each period
        cost_unit_order: list of integers, representing the unit ordering cost in each period 
        cost_unit_hold: list of integers, representing the unit holding cost in each period
        penalty: list of integers, representing the unit backlogging penalty in each period
        service_level: integer, representing the predetermined service level
        
    Returns:
        total_cost: an integer, represents the minimal total cost of all order lot size
    """
    total_cost = 0
    return total_cost
