def fleet_routing(locations, planes, periods, capacity, cost, available_planes, delta, number_of_passengers):
    """
    Args:
        locations: list of integers, representing different locations/cities
        planes: list of integers, representing different types of planes
        periods: list of integers, representing different periods
        capacity: list of integers, representing the capacity of each type of plane
        cost: list of integers, representing the cost of using each type of plane
        available_planes: list of integers, representing the number of available planes for each type
        delta: 4-dimensional list, representing the flight schedule (binary parameter)
        number_of_passengers: 4-dimensional list, representing the number of passengers for each flight
        
    Returns:
        total_cost: an integer, representing the minimum total cost of using planes
    """
    # To be implemented
    total_cost = 0
    return total_cost