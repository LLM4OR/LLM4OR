def tsp(supply, demand, cost):
    """
    Args:
        supply: a list of integers, indicates the amount of supply available at each origin.
        demand: a list of integers, indicates the amount of demand required at each destination.
        cost: a 2D list of integers, indicates the transportation cost from each origin to each destination.

    Returns:
        total_cost: an integer, denotes the minimum total transportation cost.
    """
    # To be implemented
    total_cost = 0
    return total_cost